{
  "name": "eight0sixandcompany-functions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --platform=node --target=node20 --outfile=lib/index.js --external:firebase-admin --external:firebase-functions --jsx=automatic",
    "build:watch": "npm run build -- --watch",
    "predeploy": "npm run build"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "@react-email/render": "latest",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "resend": "^3.0.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
